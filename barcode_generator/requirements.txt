# Runtime dependencies (keep in sync with pyproject.toml)
python-barcode>=0.15.1
Pillow>=11.0.0

# Dev / test
pytest>=8.0
pytest-cov>=6.0
ruff>=0.9.0
mypy>=1.14
pre-commit>=4.0

# Packaging
pyinstaller>=6.0
